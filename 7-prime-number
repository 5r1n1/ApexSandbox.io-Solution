public Boolean isPrime(Integer num) {
    Boolean isPrime = true;
    for (Integer i = 2; i <= Math.sqrt(num) && isPrime; i++)
        if (math.mod(num, i) == 0) isPrime = false;
    return isPrime;
}